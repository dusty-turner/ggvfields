## Submission
This is a patch release (1.0.1) that fixes a bug where the `length` legend
for `geom_vector_field2()` and related stream geoms was not drawn, plus
smaller fixes (see NEWS.md).
## R CMD check results
0 errors | 0 warnings | 0 notes
## Reverse dependencies
There are currently no reverse dependencies.
